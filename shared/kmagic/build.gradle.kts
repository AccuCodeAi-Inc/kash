plugins {
    id("kash.kmp")
}

description =
    "kash shared kmagic — pure-Kotlin `file(1)`-style content-type detection by magic-number signatures, with carving/binwalk scanning support, used by `file` and archive tools."

package com.accucodeai.kash.tools.od

import com.accucodeai.kash.api.CommandSpec

/** Command specs exposed by the `od` subsystem. */
public val odCommands: List<CommandSpec> =
    listOf(
        OdCommand(),
    )
